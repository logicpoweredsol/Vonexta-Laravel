










/*!
 * Column visibility buttons for Buttons and DataTables.
 * 2016 SpryMedia Ltd - datatables.net/license
 */

(function( factory ){
	if ( typeof define === 'function' && define.amd ) {
		// AMD
		define( ['jquery', 'datatables.net', 'datatables.net-buttons'], function ( $ ) {
			return factory( $, window, document );
		} );
	}
	else if ( typeof exports === 'object' ) {
		// CommonJS
		module.exports = function (root, $) {
			if ( ! root ) {
				root = window;
			}

			if ( ! $ || ! $.fn.dataTable ) {
				$ = require('datatables.net')(root, $).$;
			}

			if ( ! $.fn.dataTable.Buttons ) {
				require('datatables.net-buttons')(root, $);
			}

			return factory( $, root, root.document );
		};
	}
	else {
		// Browser
		factory( jQuery, window, document );
	}
}(function( $, window, document, undefined ) {
'use strict';
var DataTable = $.fn.dataTable;

$.extend(DataTable.ext.buttons, {
    statusFilter: function(dt, conf) {
        return {
            text: 'Status',
            extend: 'collection',
            autoClose: true,
            buttons: [{
                    text: 'All',
                    action: function(e, dt, button, conf) {
                        dt.column(4).search('').draw(); // Assuming status column is at index 4, adjust accordingly
                    }
                },
                {
                    text: 'Active',
                    action: function(e, dt, button, conf) {
                        dt.column(4).search('Active').draw(); // Assuming status column is at index 4, adjust accordingly
                    }
                },
                {
                    text: 'Inactive',
                    action: function(e, dt, button, conf) {
                        dt.column(4).search('Inactive').draw(); // Assuming status column is at index 4, adjust accordingly
                    }
                }
            ]
        };
    }
});


return DataTable.Buttons;
}));

